class Shopitem{

  int id;
  String name;
  String category;
  int points;
  String value;
  String img;
  String info;

  Shopitem(this.id, this.name, this.category, this.points, this.value, this.img, this.info);
}